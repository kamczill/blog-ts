export interface onImageLoadProps {
    onImageLoad: () => void;
}

export interface viewAllButtonProps {
    classes: string
}

export interface headingProps {
  title: string;
  description: string;
  showButton?: boolean;
}